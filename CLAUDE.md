# GROUND RULES (모든 작업 공통)

## 작업 방식
- 작업 시작 전: 계획 3~5줄 먼저 보여주고 승인 후 코딩
- 한 번에 한 가지만 수정. 요청받은 범위만 ("겸사겸사" 리팩토링 금지)
- 기존 코드 스타일/네이밍 따를 것

## 커밋
- 논리적 단위마다 커밋. 30줄 넘으면 쪼개기
- 커밋 메시지: "무엇을" + "왜"
- 큰 수정 전 반드시 현재 상태 커밋 (복구 지점)

## 에러 처리
- 모든 외부 호출에 실패 케이스 처리
- 빈 값 / null / 타임아웃 3종 세트 체크
- 에러 삼키지 말고 로그 남기기

## 테스트
- 핵심 로직 수정 시 테스트 통과 확인
- "될 겁니다" 금지. 실행 결과로 증명

## 코드 품질
- 함수 하나 = 역할 하나. 50줄 넘으면 분리 검토
- 변수명은 6개월 뒤에도 읽히게. 매직넘버 금지
- 주석은 "왜"만 설명

## 소통
- 결론부터. 손익/리스크 중심
- 확실하지 않으면 물어보기
- 완료 보고: 변경 파일 + 핵심 변경점 + 테스트 결과 3줄
- 기술 용어는 비개발자 언어로 번역

## 금지
- 요청 없는 의존성 추가
- API 키/비밀번호 하드코딩
- 검증 없이 프로덕션 데이터 수정
- 테스트 실패 상태로 "완료" 선언

---

# GAS 전용 룰 (.gs 파일 작업 시 적용)

## 시트 접근
- 시트명/범위 하드코딩 금지 → 파일 상단 CONFIG 상수로 모으기
- getRange 반복 호출 금지 → getValues()로 한 번에 읽고 setValues()로 한 번에 쓰기

## 실행 한계 대응
- GAS 실행 제한 6분 → 대량 처리 시 배치 분할 + 진행상태 저장
- UrlFetchApp 호출엔 muteHttpExceptions: true + 응답코드 체크 필수

## 로깅/디버깅
- 주요 분기마다 Logger.log 남기기
- try-catch에서 에러 무시 금지. 에러 메시지 + 함수명 기록

## 배포 안전
- onEdit/onOpen 트리거 함수는 가볍게 유지
- 실데이터 수정 전 테스트 시트에서 먼저 검증
- API 키는 Properties Service에 저장 (코드에 직접 X)

## 버전 관리
- clasp 사용 시 push 전 커밋 필수
- 수동 관리 시 큰 수정 전 버전 저장 또는 사본 생성
